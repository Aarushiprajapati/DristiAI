// ─────────────────────────────────────────────────────────────
//  App Theme – Dark High-Contrast Design System
// ─────────────────────────────────────────────────────────────

export const colors = {
    // Dark backgrounds
    bg: '#0a0a1a',
    bgCard: '#12122a',
    bgElevated: '#1a1a35',

    // Accent
    primary: '#6C63FF',      // Electric violet
    primaryLight: '#8B85FF',
    primaryDark: '#4A44C6',

    // Status
    safe: '#00E676',         // Bright green
    warning: '#FFAB00',      // Amber
    danger: '#FF1744',       // Red

    // Text
    textPrimary: '#FFFFFF',
    textSecondary: '#B0B0C8',
    textMuted: '#6B6B8A',

    // UI elements
    border: '#2A2A4A',
    overlay: 'rgba(0,0,0,0.7)',
    cardShadow: 'rgba(108,99,255,0.2)',

    // SOS
    sos: '#FF1744',
    sosLight: '#FF5252',
};

export const spacing = {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
    xxl: 48,
};

export const radius = {
    sm: 8,
    md: 16,
    lg: 24,
    full: 999,
};

export const fontSize = {
    xs: 12,
    sm: 14,
    md: 16,
    lg: 18,
    xl: 22,
    xxl: 28,
    xxxl: 36,
};

export const fontWeight = {
    regular: '400',
    medium: '500',
    semibold: '600',
    bold: '700',
    extrabold: '800',
};
